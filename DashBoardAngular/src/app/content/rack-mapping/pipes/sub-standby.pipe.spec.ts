import { SubStandbyPipe } from './sub-standby.pipe';

describe('SubStandbyPipe', () => {
  it('create an instance', () => {
    const pipe = new SubStandbyPipe();
    expect(pipe).toBeTruthy();
  });
});
